# Ensure package-lock is generated with exact versions
package-lock=true
# Use Node.js 18
engine-strict=true
# Disable package-lock.json generation
package-lock=false
# Use npm's legacy peer deps to avoid peer dependency issues
legacy-peer-deps=true
